# Kitty Library
This is just a practice project of FastAPi. [ CRUD Operation (dummy database) and Well structure ]

## How to start
just clone the project 😎 and
run:
```bash
#Install/check your dependencies (you already have them, but torch and torchvision are key):
pip install -r requirements.txt
#Run The Server
fastapi dev src/
```
## Api doc
```bash
http://127.0.0.1:8000/docs
```
